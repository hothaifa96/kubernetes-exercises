{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend aggregator — serves dashboard + /ready with all backend pod IPs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
