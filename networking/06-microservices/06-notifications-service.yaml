# ============================================================
# LAB 6 — Part 6: Notifications Service (Deployment + ClusterIP Service)
# ============================================================
# Same sidecar pattern as the other backend services.
#
# DEPLOY:
#   kubectl apply -f 06-notifications-service.yaml
#
# VIEW LOGS:
#   kubectl logs -l app=notifications-service -c app
#   kubectl logs -l app=notifications-service -c logger
#
# HIT THE /ready ENDPOINT:
#   kubectl port-forward svc/notifications-service 8084:8080
#   curl http://localhost:8084/ready
# ============================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-service
  labels:
    lab: "06-microservices"
    app: notifications-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications-service
  template:
    metadata:
      labels:
        app: notifications-service
        lab: "06-microservices"
    spec:
      volumes:
      - name: log-vol
        emptyDir: {}
      - name: scripts
        configMap:
          name: service-scripts
          items:
          - key: server.py
            path: server.py

      containers:
      - name: app
        image: python:3.11-slim
        command: ["python", "/app/server.py"]
        env:
        - name: SERVICE_NAME
          value: "notifications-service"
        - name: PORT
          value: "8080"
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        ports:
        - containerPort: 8080
          name: http
        volumeMounts:
        - name: scripts
          mountPath: /app
        - name: log-vol
          mountPath: /var/log/app
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"

      - name: logger
        image: busybox:1.36
        command: ["/bin/sh", "-c"]
        args:
        - |
          mkdir -p /var/log/app
          touch /var/log/app/access.log
          echo "[logger-sidecar] Watching /var/log/app/access.log ..."
          exec tail -F /var/log/app/access.log
        volumeMounts:
        - name: log-vol
          mountPath: /var/log/app
        resources:
          requests:
            cpu: "10m"
            memory: "16Mi"
          limits:
            cpu: "50m"
            memory: "32Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  labels:
    lab: "06-microservices"
    app: notifications-service
spec:
  type: ClusterIP
  selector:
    app: notifications-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
