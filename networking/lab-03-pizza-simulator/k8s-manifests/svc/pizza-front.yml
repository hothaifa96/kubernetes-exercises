apiVersion: v1
kind: Service
metadata:
  namespace: pizza
  name: pizza-dashboard
spec:
  type: NodePort
  selector:
    app: pizza-dashboard
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 32001
